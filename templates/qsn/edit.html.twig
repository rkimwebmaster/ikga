{% extends 'base.html.twig' %}

{% block title %}Edit QSN{% endblock %}

{% block body %}
    <h1>Edit QSN</h1>

    {{ include('qsn/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_q_s_n_index') }}">back to list</a>

    {{ include('qsn/_delete_form.html.twig') }}
{% endblock %}
